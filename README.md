This code contains both the client and server side application for accepting payments using Stripe.

To run the application, all you have to do is execute the following command:
**npm i && npm start**

